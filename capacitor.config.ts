import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'project_group1',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
